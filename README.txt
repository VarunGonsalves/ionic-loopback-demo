This is a simple demo for connecting loopback server to an ionic application.
I have a loopback server that connects to a mongoDB database.
It communicates with the ionic app via REST API.

The ionic application can be used to view, add, update or delete student data.